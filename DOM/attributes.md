VALUE = serves to read checked inputs, text inputs, such as passwords and usernames, measurements etc etc (whatever the user will INPUT), as well you can change it

RANGE = working on inputs u can set a range to measure something, so for example a contrast setting for image on your laptop, and it goes from 0  - 100. You can work the range to go from 10un to 10un, u can check and change the range that has been input by user, etc

CHECKED = Serves for check boxes, is boolean so it will return true or false.

PLACEHOLDER = serves to change the text inside the input 

HREF = Attribute that u can access it easily and change the link from thr JS script u can also set phone numbers and emails not just regular url
phone: use "tel:"
mail: use "mailto:"
