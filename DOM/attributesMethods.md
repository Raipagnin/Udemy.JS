- getAttribute() = wirte inside () the name of the att you wanna get in order to have access and it will return the value or Null

- setAttribute() = u pass more than one argument where the first one is gonnna the att and the second being the value to be changed
NOTE that both of these u have to pass the arguments as strings:
>> range.getAttribute('max')
or
>> range.setAttribute('min', '0')

